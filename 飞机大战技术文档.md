# 飞机大战 Web 小游戏 · 技术实现文档

> 目标读者：solo builder（单人开发者 / AI 编码代理）
> 本文档用于指导从零实现一个**科技风、玩法丰富、含大 Boss**的竖版飞机射击网页游戏。
> 文档只描述"要做什么、怎么做、做成什么样"，具体代码由 builder 生成。

---

## 1. 项目概述

### 1.1 一句话定位
一款**竖版弹幕射击（STG）**网页小游戏，融合市面上爆火飞机大战的核心玩法，主打**赛博朋克 / 未来科技**视觉，含**多阶段大 Boss 战**。

### 1.2 玩法类型融合（对标市面产品）

| 对标产品 | 借鉴的核心玩法 | 本项目落地方式 |
|---------|--------------|--------------|
| 微信《飞机大战》 | 火力升级、三种主武器切换 | 击杀掉落道具，主武器可升级，副武器/僚机跟随 |
| 《雷霆战机》 | 装备位、护盾、追踪弹、能量槽 | 护盾、追踪导弹、僚机、护甲值 |
| 《雷电》/ 东方弹幕 | 密集弹幕、擦弹、Boss 阶段 | Boss 弹幕阶段、自机小判定点、擦弹得分 |
| 街机竖版 STG | 关卡 + 无尽模式、波次刷怪 | 关卡模式（固定 Boss）+ 无尽模式（分数冲榜） |

### 1.3 核心体验关键词
- **爽快**：连击、爆炸粒子、屏幕震动、行云流水的弹幕。
- **有挑战**：Boss 多阶段、弹幕花样多、敌人阵型多样。
- **美观**：霓虹发光、星空/网格滚动背景、扫描线、粒子特效。
- **成长**：火力升级、道具收集、分数/连击系统。

---

## 2. 技术栈选型（推荐）

| 项 | 方案 | 说明 |
|----|------|------|
| 渲染 | **HTML5 Canvas 2D** | 2D STG 足够，性能可控，无框架学习成本 |
| 语言 | **原生 JavaScript（ES6+）** | 单文件或少量模块，便于 solo builder 一次性生成可靠代码 |
| 画面分层 | 多层 Canvas 或单 Canvas 分层绘制 | 背景层 / 游戏层 / UI 层 |
| UI/HUD | **DOM + CSS**（霓虹风格） | 菜单、血条、弹窗用 DOM 更灵活 |
| 音频 | **Web Audio API**（程序化音效） | 无需音频资源，激光/爆炸音用合成音 |
| 部署 | 纯静态，双击打开或任意静态服务器 | 无构建依赖，开箱即用 |

> **备选方案**：若 builder 倾向框架，可用 `Phaser 3`（CDN 引入）。本文档默认走原生 Canvas 方案，架构设计两种都兼容。

### 2.1 无需外部依赖清单
- 无需图片资源：所有图形用 Canvas 绘制（发光用阴影/渐变/`globalCompositeOperation = "lighter"`）。
- 无需音频文件：用 Web Audio 合成。
- 无需构建工具：浏览器直接运行。

---

## 3. 项目结构

建议采用**单入口 + 按职责拆分文件**（也可合并成单 HTML 文件，builder 自选）：

```
飞机大战/
├── index.html          # 入口页面，含 Canvas 与 UI 骨架、CSS
├── css/
│   └── style.css       # 科技风 UI 样式、霓虹、扫描线
├── js/
│   ├── main.js         # 启动入口、游戏循环、状态机
│   ├── config.js       # 全部数值/弹幕/Boss 配置（数据驱动）
│   ├── input.js        # 键盘/鼠标/触摸输入
│   ├── scene.js        # 场景背景（星空、网格、滚动）
│   ├── entities.js     # 实体基类：Player / Enemy / Boss / Bullet / Item
│   ├── bullet.js       # 弹幕与弹道系统（含 Boss 弹幕生成器）
│   ├── enemy.js        # 敌人类型 + 阵型 + 行为
│   ├── boss.js         # Boss 状态机与多阶段攻击
│   ├── item.js         # 道具掉落与拾取
│   ├── effects.js      # 粒子、爆炸、屏幕震动、连击提示
│   ├── audio.js        # Web Audio 合成音效
│   ├── hud.js          # 分数/血条/Boss血条/连击 UI
│   └── screen.js       # 启动页 / 暂停 / 结算 / 关卡过渡
└── 飞机大战技术文档.md
```

> 若做成**单文件** `index.html`，按上面模块顺序编写即可，逻辑不变。

---

## 4. 核心架构

### 4.1 游戏主循环
- 用 `requestAnimationFrame` 驱动，配合 `deltaTime`（毫秒）保证不同刷新率下速度一致。
- 固定逻辑更新 + 渲染分离：
  - `update(dt)`：更新实体、碰撞、输入。
  - `render(ctx)`：绘制背景、实体、特效、UI。

```
loop(now):
  dt = now - lastTime
  handleInput()
  update(dt)
  checkCollisions()
  spawnLogic()
  render()
  rAF(loop)
```

### 4.2 游戏状态机（GameState）
```
MENU → PLAYING ⇄ PAUSED → GAME_OVER / VICTORY → MENU
       └─ LEVEL_TRANSITION（关卡过渡动画）
```

### 4.3 对象池（Object Pool）
- 所有高频生成对象（子弹、敌人、粒子、爆炸）使用对象池复用，避免 GC 卡顿。
- `pool.acquire() / pool.release(obj)`。
- 对象统一带 `active` 标志位，未激活的不参与更新与绘制。

### 4.4 碰撞检测
- **圆形碰撞**为主（速度快、够用）：`dist(c1, c2) < r1 + r2`。
- 命中判定可缩小玩家判定半径（自机小判定点 = "擦弹"手感，半径约自机贴图的 20%~30%）。
- 空间优化：子弹/敌人数多时按 Y 分桶，或先做 AABB 粗筛再做精判。

### 4.5 数据驱动
- 所有敌人、弹幕、Boss、波次、道具数值集中在 `config.js`，builder 只改数据即可调平衡。

---

## 5. 系统详细设计

### 5.1 玩家系统（Player）

| 属性 | 说明 |
|------|------|
| 生命 / 护盾 | 生命 3 条；护盾可吸收 N 次伤害 |
| 移动 | 8 方向，键盘 + 移动端触摸拖拽；移动平滑（加速度/阻尼）|
| 主武器 | 初始 1 级，可升至 5 级 |
| 副武器 | 僚机（跟随）、追踪导弹、护盾、时间减速等（最多装备位）|
| 无敌时间 | 受击后短暂无敌（闪白） |
| 大招 / 炸弹 | 攒能量释放全屏清屏（Bomb） |

**推荐主武器升级路径**：
```
Lv1: 单发直射
Lv2: 双发
Lv3: 三发（中间 + 两侧斜射）
Lv4: 三发 + 后方僚机子弹
Lv5: 五发扇形 + 追踪弹
```
- 火力升级后有过热/冷却（cooldown），等级越高射速越快。

### 5.2 敌人系统（Enemy）

| 类型 | 行为 | 血量 | 分值 |
|------|------|------|------|
| 小兵（Fodder）| 直线下降 / 轻微左右摆动 | 低 | 低 |
| 冲锋兵（Diver）| 冲入后瞄准玩家俯冲 | 低 | 中 |
| 炮台（Turret）| 定点发射定向弹 | 中 | 中 |
| 精英（Elite）| 扇形弹幕 + 较厚血 | 高 | 高 |

**阵型（Formation）**：直线、V 字、圆环、左右交错，按波次调度进场。

### 5.3 子弹/弹幕系统（Bullet）
- **玩家子弹**：直线、扇形、追踪、穿透、激光（持续判定）。
- **敌方弹幕**：直线、螺旋、扇形、环形、瞄准、散射、激光预警（先描线再发射）。
- 每颗子弹记录：位置、速度/方向、类型、来源阵营、伤害、是否穿透。

### 5.4 道具系统（Item）
击杀敌人概率掉落：

| 道具 | 效果 |
|------|------|
| P（火力）| 主武器升级 |
| 僚机（Option）| 增加副武器僚机 |
| 追踪导弹（M）| 自动追踪导弹 |
| 护盾（S）| 获得/补充护盾 |
| 炸弹（B）| 增加大招次数 |
| 星星（Score）| 加分 |

---

## 6. Boss 战设计（重点）

### 6.1 Boss 通用结构
- 进入 Boss 战有**登场动画 + 血条 UI**。
- Boss 有**多个阶段**，血量降到阈值切换阶段，切换时清屏子弹、短暂无敌、特效过渡。
- Boss 攻击循环：`阶段1 → 阶段2 → 阶段3（狂暴）→ 击破`。

### 6.2 三阶段攻击推荐方案

**阶段 1（90% ~ 60% 血量）——试探**
- 扇形弹幕 + 瞄准玩家单发。
- 周期性召唤 2 架僚机护卫。

**阶段 2（60% ~ 30% 血量）——弹幕强化**
- 螺旋弹幕（旋转发射）。
- 环形扩散弹。
- 定向激光（先预警线再扫射）。

**阶段 3（30% ~ 0 血量）——狂暴**
- 弹幕速度、密度翻倍。
- 连续激光扫射 + 全屏弹幕间隙。
- 移动速度加快，压迫感加强。

### 6.3 Boss 数值示例（可在 config 调整）

```
boss: {
  hp: 3000,
  phases: [
    { hpPct: 0.60, pattern: "spiral",   fireRate: 1200 },
    { hpPct: 0.30, pattern: "ring",     fireRate: 900  },
    { hpPct: 0.00, pattern: "enrage",   fireRate: 500  }
  ],
  moveRange: { x: [80, canvasW-80], y: [60, 300] },
  score: 50000
}
```

### 6.4 Boss 血条 UI
- 顶部独立血条，分段显示阶段，阶段切换时颜色变化 + 闪烁提示。

---

## 7. 关卡 / 模式设计

### 7.1 关卡模式
- N 个关卡，每关：若干波小兵/精英 → 中间过渡 → 关底 Boss。
- 波次由 `config.js` 的 `level: { waves: [], boss: {} }` 驱动。

### 7.2 无尽模式
- 敌人无限生成，难度随时间指数上升，冲最高分。
- 每 60 秒或每 5000 分刷一次精英/Boss。

### 7.3 计分与连击
- **连击系统**：连续击杀在 X 秒内不断连击，连击倍率递增（×1 → ×5）。
- **擦弹得分**：敌方子弹擦过自机小判定点边缘给予额外分数和特效反馈。
- 保留本地最高分（`localStorage`）。

---

## 8. 视觉风格规范（科技感核心）

### 8.1 配色方案（赛博朋克）
| 用途 | 颜色 |
|------|------|
| 背景深空 | `#05070f` / `#0a0e1a` |
| 主霓虹 | `#00f0ff`（青）|
| 强调霓虹 | `#ff2d95`（品红）|
| 辅助霓虹 | `#7c4dff`（紫）|
| 玩家/友军 | 青 / 天蓝 |
| 敌人 | 品红 / 橙红 |
| 警告/红色 | `#ff3b3b` |

### 8.2 核心视觉手法
1. **发光**：Canvas `shadowBlur` + `globalCompositeOperation = "lighter"` 叠加发光。
2. **滚动背景**：星空粒子 + 透视网格（近大远小）向下滚动，营造速度感。
3. **扫描线/暗角**：CSS `overlay` 层加扫描线与 vignette。
4. **粒子特效**：爆炸（碎片 + 光点）、引擎尾焰、受击火花。
5. **屏幕震动**：爆炸 / Boss 全屏攻击时轻微 shake。
6. **动效大纲**：UI 元素加霓虹边框、`text-shadow` 发光。

### 8.3 画面分层（绘制顺序）
```
背景层（星空 + 网格）
  ↓
实体层（敌人 / 玩家 / 子弹）
  ↓
特效层（粒子 / 爆炸 / 激光）
  ↓
UI 层（DOM：HUD / 血条 / 弹窗）
  ↓
氛围层（扫描线 / 暗角）
```

---

## 9. UI / UX 设计

### 9.1 画面清单
| 画面 | 内容 |
|------|------|
| 启动页 | 游戏标题（发光）、模式选择、开始按钮、最高分 |
| HUD | 分数、连击倍率、生命/护盾、火力等级、大招能量、Boss 血条 |
| 暂停 | 继续 / 重新开始 / 返回菜单 |
| 结算 | 胜利/失败、总分、连击最高、评价（S/A/B/C）|
| 关卡过渡 | "STAGE X" 大字 + 倒计时 |

### 9.2 交互反馈
- 受击：屏幕红闪 + 玩家闪白无敌。
- 连击：屏幕边缘连击数字跳动 + 音调渐升。
- 拾取道具：粒子吸附 + 提示文字。
- 大招：全屏白闪 + 清屏爆炸。

---

## 10. 音效设计（Web Audio 合成，无资源文件）

| 事件 | 合成方式 |
|------|---------|
| 玩家射击 | 短促方波 + 快速降频的"biu" |
| 敌机爆炸 | 白噪声衰减（noise burst）|
| Boss 爆炸 | 低频正弦 + 长衰减 + 低通 |
| 拾取道具 | 上行琶音 |
| 连击提升 | 音调随连击数升高 |
| 大招 | 全频扫频（sweep）+ 冲击 |

- 提供 `audio.play(name)` 接口，音效池化防重复实例超载。
- 背景音乐可选：简单循环合成琶音（进阶项，不强求）。

---

## 11. 性能优化

1. **对象池**：子弹/粒子/敌人全部复用。
2. **减少 shadowBlur**：发光特效适量使用，或预渲染到离屏 canvas 缓存。
3. **合成层分离**：背景和特效考虑离屏渲染。
4. **裁剪**：超出屏幕的对象立即回收，不参与绘制。
5. **帧率自适应**：满帧运行；低端设备可降级粒子数量。
6. **避免频繁 DOM 操作**：HUD 数值用变量缓存，变化时才更新 DOM。

---

## 12. 验收清单（builder 完成后自检）

- [ ] 游戏可正常启动，菜单可进入两种模式。
- [ ] 玩家 8 方向移动流畅，键盘 + 触摸均可用。
- [ ] 主武器 5 级可升级，效果可见。
- [ ] 敌人阵型敌弹可见，能正常被击杀。
- [ ] 道具掉落与拾取生效（火力/僚机/导弹/护盾/炸弹/得分）。
- [ ] Boss 三阶段切换正确，血条显示正确，击破有结算。
- [ ] 碰撞判定准确，自机有小判定点（擦弹手感）。
- [ ] 受击无敌、生命/护盾扣除正确。
- [ ] 连击/计分/最高分存档正常。
- [ ] 粒子、爆炸、屏幕震动、扫描线等视觉特效正常。
- [ ] 音效有且不刺耳、不卡顿。
- [ ] 暂停/结算/返回菜单流程正确，无内存泄漏（长时运行不卡）。
- [ ] 满帧运行，低端设备无明显掉帧。

---

## 13. 后续扩展预留（可选）

- 双人模式、无尽排名榜。
- 更多 Boss、更多弹幕类型。
- 武器皮肤/战机选择。
- 移动端虚拟摇杆。
- 本地存档多档案。

---

## 附：关键实现提示（给 builder）

1. 用 `globalCompositeOperation = "lighter"` 叠加发光，但粒子层单独控制，避免整体变糊。
2. 敌方弹幕用"发射器"抽象：`spawnPattern(type, origin, params)`，Boss 阶段只是换 pattern。
3. 玩家判定点务必小于贴图，这是弹幕游戏手感的关键。
4. 所有数值集中到 `config.js`，builder 不要硬编码到逻辑里。
5. 优先保证 60fps 与手感，再叠加视觉特效。